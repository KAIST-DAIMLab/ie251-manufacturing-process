services:
  noetic:
    build: .
    image: dev-ros-noetic
    container_name: noetic
    hostname: noetic
    network_mode: host
    ipc: host
    stdin_open: true
    tty: true
    environment:
      - DISPLAY=${DISPLAY}
      - QT_X11_NO_MITSHM=1
      - XAUTHORITY=/tmp/.docker.xauth
      - ROS_HOSTNAME=192.168.0.2
      - ROS_MASTER_URI=http://192.168.0.2:11311
    volumes:
      - noetic-home:/home/ubuntu
      - ..:/home/ubuntu/workspace
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - /tmp/.docker.xauth:/tmp/.docker.xauth:rw
    devices:
      - /dev/dri:/dev/dri
    group_add:
      - video
    working_dir: /home/ubuntu/workspace
    command: /bin/zsh

volumes:
  noetic-home:
